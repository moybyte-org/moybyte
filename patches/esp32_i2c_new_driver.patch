diff --git a/ports/esp32/mpconfigport.h b/ports/esp32/mpconfigport.h
index 55503ff..1122334 100644
--- a/ports/esp32/mpconfigport.h
+++ b/ports/esp32/mpconfigport.h
@@ -195,7 +195,7 @@
 #define MICROPY_HW_SOFTSPI_MIN_DELAY        (0)
 #define MICROPY_HW_SOFTSPI_MAX_BAUDRATE     (esp_rom_get_cpu_ticks_per_us() * 1000000 / 200) // roughly
 #ifndef MICROPY_HW_ESP_NEW_I2C_DRIVER
-#define MICROPY_HW_ESP_NEW_I2C_DRIVER       (0)
+#define MICROPY_HW_ESP_NEW_I2C_DRIVER       (1) /* Moybyte #69: the legacy driver's timeout= bounds ONE clock-stretch event (S3 exponential reg) inside a hardcoded 100ms*(1+len) transaction wait, so a slave stretching many sub-cap times stalls a read 40-60ms "successfully". The new i2c_master driver passes timeout as the PER-TRANSACTION cap -- the bound machine.I2C(timeout=) documents. */
 #endif
 #define MICROPY_PY_SSL                      (MICROPY_PY_NETWORK)
 #define MICROPY_SSL_MBEDTLS                 (MICROPY_PY_SSL)
